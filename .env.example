# Supabase project credentials.
# When these are missing or left as placeholders, the app gracefully falls back
# to a fully-functional offline localDb mode backed by browser localStorage.
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
